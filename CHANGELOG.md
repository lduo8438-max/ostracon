# 變更記錄

版本號遵循 [Semantic Versioning](https://semver.org/lang/zh-TW/)。

**這裡只記對使用者可見的變更。** 內部量測、重構與設計理由在 `docs/status.md`，
那份是逐刀的完整紀錄，不是給使用者讀的。

判斷「可見」的標準有三條，符合任一條就要記：
指令與旗標的行為、**產出的內容**（同一份 repo 會不會印出不同的答案）、
以及資料庫的相容性（既有索引還能不能用）。

---

## [0.1.2] — 2026-09-03

**誠實性與互動修補。** 索引、匹配與雜湊一行未動，schema 仍是 v3，
`why` / `ostracised` / `hotspots` 的產出逐字相同，0.1.0 之後建的索引直接可用。

這一版的來源是**拿工具跑一個沒調過的 repo**（pypa/pip，16,241 commit、
21,409 個宣告）。那一趟暴露的問題有些是設計題、有些是明確缺陷；
**這裡只收明確缺陷**，設計題留到下一個里程碑。

### 修正：說了不成立的話

- **宣告選單其實是策展過的，而畫面與 README 都說得像全部。** 頁尾原本寫
  「Showing 60 of 400」，讀起來像 400 就是這個語料的全部。實測 pypa/pip 有
  **21,409 個宣告，`ostracon ui` 的清單只有 400 筆（1.9%）**，光是「有可呈現
  理由」的就有 4,243 個。現在清單少於全部時，頁尾會明說它是策展過的、
  印出真正的總數，並指向沒有這個限制的 `ostracon why`。
  **README 那句「任何一個宣告都查得到」是錯的，已改掉。**
  （全語料搜尋還沒做，這一版只是不再把限制藏起來。）

### 修正：空清單讓畫面全黑

- **匹配階梯**在一條配對都沒有時整頁空白。它是載入時的預設畫面，所以只有
  一顆 commit 的 repo（全部是誕生、零配對）一開起來就是黑的。零配對是合法
  的索引結果，現在會說明它是結果不是缺口。

### 修正：互動與鍵盤

- **宣告選單現在是一個真的 modal**：`Escape` 在焦點不在輸入框時也能關
  （先前只綁在輸入框上，焦點一移到任何一列就失效）、Tab 不會離開對話框、
  關閉之後焦點回到打開它的那個按鈕、加上 `aria-modal`。
- **拿掉兩個假按鈕**：階梯徽章與側欄的 repo 區塊都是 `<button>` 卻沒有任何
  動作——可以被 Tab 停留、有按下去的樣子，按了什麼都不會發生。
- **熱點列的 Enter 與 Space 都能開**。`role="button"` 的合約是兩者皆可，
  而先前只吃 Enter；Space 在可聚焦元素上的預設行為是捲動頁面，鍵盤使用者
  按下去只會看到畫面跳走。
- **理由跳轉的第一下會去第一條理由**。標頭寫「1 / N」宣稱游標在第一條上，
  而按下去卻算 `(0+1) % N` 跳到第二條——第一條要繞完一圈才看得到。
- **從熱點或被推翻清單點進時間軸之後，上一頁回得去**。那一跳原本用
  `replaceState`，所以沒有留下歷史，按上一頁會直接離開站台。時間軸**內部**
  的理由跳轉仍然用 `replaceState`：那是游標移動不是導覽，不該堆歷史。

### 沒有動的

扇出、大集合的排序與資訊架構、全語料搜尋、mobile 版時間軸重排，以及長篇
commit body 暴露的引文邊界問題（762 條引文裡 124 條跨過句號還接下去）。
**那些要兩套陌生 repo 的結果才決定得了**，現在動等於用一套語料的形狀去定規格。

---

## [0.1.1] — 2026-09-03

**只有 `ostracon ui` 與 `ostracon export` 這一側改變。** 索引、匹配與雜湊一行
未動（`src/index` / `src/match` / `src/git` / `db` 的唯一差異是把 `touches`
從 private 改成 export，讓畫面與 matcher 用同一個判準——邏輯零改動）。
所以同一份 repo 印出的 `why` / `ostracised` / `hotspots` 逐字相同。

### 指令與旗標

- **`ostracon export --repo <語料路徑>`（新增，選用）** — 把身份斷層的前後
  程式碼片段嵌進匯出的站台。**預設不嵌入**：索引本身不存原始碼，只有 blob
  雜湊與 byte 位移，所以片段是匯出時回讀該語料的 git 物件切出來的。
  片段最多 24 行 / 4,000 bytes，超過會標示原始行數。
  **那些程式碼會隨站台公開**，所以它是明示的旗標而不是預設行為。
- 沒有旗標被移除或改變語意。

### 產出的內容

- **`ostracon ui` 換成新的工作台**：五個畫面（匹配階梯、時間軸、身份斷層、
  攪動熱點、被推翻的做法）取代原本的三欄頁面。按 `/` 開宣告選單，**任何一個
  宣告都查得到**，不再限於策展過的那幾條。
- **每一條時間軸有固定網址**（`#<stable_key>`，可再帶 `/<short-sha>` 指到某一
  次改動）。舊的純 key 形式繼續有效。
- **匯出的站台多了三個端點**：`api/ladder.json`、`api/discontinuities.json`、
  `api/hotspots.json`。既有三個端點的路徑與內容不變。
- 斷層畫面會說明片段為什麼在或不在，三種狀態分開：已嵌入、未要求嵌入、
  語料讀不到。**「沒有片段」與「沒有程式碼」是兩件事。**

### 修正

- **匯出的站台掛在子目錄之下時整個讀不到資料。** 資產與資料端點都寫成
  網域根絕對路徑，於是 `https://example.github.io/<repo>/<語料>/` 底下的頁面
  會去 `https://example.github.io/api/*.json` 抓，六個端點全部 404。
  現在一律相對於頁面解析。**這是 `ostracon export` 的主要用途，等於它先前
  只在網域根上可用。**
- **空清單會讓畫面整個變白。** 零斷層的語料（例如沒有任何身份斷層的 repo）與
  零專屬理由的宣告都會丟出例外。零是合法的結果，不是缺口。
- 深連結指向沒有理由的那一次改動時會捲到別的地方——把「找不到」偽裝成
  「找到了」。
- 「Open its timeline」可能指向不在匯出範圍內的頁面。

### 安裝體積

畫面現在是預先建好的靜態資產（含五個子集化的自託管字體檔），跟著 `dist`
一起裝：**packed 約 286 → 約 502 kB，unpacked 約 888 → 約 1,378 kB，180 → 207 檔**
（kB 是 npm 自己用的十進位單位）。

寫成約數是因為**這個數字描述的檔案就是它自己所在的那一份**：0.1.1 發布時這裡寫
1,377.5，實際上線的是 1,377,572 bytes = 1,377.6——差的 48 bytes 正是修上一行
所增加的字。精確到 0.1 kB 在這裡是做不到的事。

**執行期相依沒有增加**——`dependencies` 仍然只有 tree-sitter 的解析器與三份
grammar。安裝端不需要 npm 以外的任何東西，也不跑任何建置。

### 資料庫相容性

**不變，仍是 v3。** 0.1.0 建的索引直接可用，不需要重建也不會就地遷移。

---

## [0.1.0] — 2026-08-30

第一次公開發布。

### 指令

- `ostracon why <path>:<symbol>` — 印出一段程式碼的演化史。預設只索引該檔案
  所屬的血緣（快）；`--full` 索引整個 repo，跨檔案搬移才看得見（慢）。
  路徑在 `--until` 已被刪除也查得到。
- `ostracon ostracised` — 列出試過又被推翻的做法。由短命到長命排序；
  測試檔的宣告預設排除，`--include-tests` 看得回來。
- `ostracon hotspots` — 列出被重構最多次的宣告。**只算真的動到結構的改動**，
  而且是 entity 層級不是檔案層級。
- `ostracon evidence extract` — 從 commit message 抽取理由並驗證 span。
  零網路、零 LLM。
- `ostracon evidence linked` — 取回被參照的 GitHub PR / issue 討論串。
  唯一會連外的指令。
- `ostracon ui` — 三欄畫面（結構 → 演化 → 意圖），只綁 `127.0.0.1`。
- `ostracon export` — 把索引匯出成純靜態站台。`--label` 必填，否則
  `summary.rootPath` 會把本機路徑公開出去。

三支查詢指令都收 `--until`，用來限制單次工作量。**產出與一次跑完相同**，
但它是可恢復、可排程的操作方式，**不是效能解法**——檢查點落在 commit 邊界。

### 語言

TypeScript、TSX、Python。加一種語言等於新增一份剖面，匹配器／雜湊／圖遍歷
／增量索引都不動。

### 環境需求

Node 24 以上，**而且它的內建 SQLite 必須含 FTS5**。沒有 FTS5 時會在建立 schema
之前明確失敗並說明怎麼辦，而不是留下 `no such module: fts5`。

### 已知限制

六條全部列在 README，不只留在註解裡。最該先知道的兩條是平行分支上的血緣歸屬，
以及 `node:sqlite` 仍是實驗性 API。

### 資料庫相容性

`schema_migration` 記錄版本。發布版是 **v3**：

- **v2 → v3 就地遷移**，不要求重建。那一版只加了一條索引，產出逐位元不變。
- **v1 → v2 拒絕續用**。那一版改了資料的存法，舊索引不可能給出正確答案。

判準是「產出會不會變」，不是「版本號有沒有變」。

[0.1.0]: https://github.com/lduo8438-max/ostracon/releases/tag/v0.1.0
